.PHONY: up build up-build down clean logs ps help

DC=docker compose
DEFAULT_SERVICE=api

# Запустить сервисы (использовать уже собранные образы)
up:
	@echo -e "\n🚀 [UP] Запуск всех сервисов... Пожалуйста, подождите ✨\n"
	$(DC) up -d
	@echo -e "\n✅ Сервисы запущены!\n"

# Собрать образы
build:
	@echo -e "\n🔨 [BUILD] Сборка образов...\n"
	$(DC) build
	@echo -e "\n✅ Сборка завершена!\n"

# Собрать образы и запустить сервисы
up-build: build up

# Остановить сервисы
down:
	@echo -e "\n🛑 [DOWN] Остановка всех сервисов...\n"
	$(DC) down
	@echo -e "\n✅ Все сервисы остановлены.\n"

# Удалить контейнеры, сети и тома
clean:
	@echo -e "\n🧹 [CLEAN] Очистка: удаление контейнеров, сетей и томов...\n"
	$(DC) down -v --remove-orphans
	@echo -e "\n✅ Очистка завершена.\n"

# Показать логи сервиса (по умолчанию: api)
logs:
	@svc=$(if $(service),$(service),$(DEFAULT_SERVICE)); \
	echo -e "\n📜 [LOGS] Логи сервиса: $$svc (Ctrl+C для выхода)\n"; \
	$(DC) logs -f $$svc

# Список всех сервисов
ps:
	@echo -e "\n🐳 [PS] Список сервисов и их статус...\n"
	$(DC) ps

# Показать доступные команды
help:
	@echo -e "\n📖 Доступные команды:\n"
	@echo "  make up        Запустить сервисы (использовать собранные образы)"
	@echo "  make build     Собрать образы"
	@echo "  make up-build  Собрать образы и запустить сервисы"
	@echo "  make down      Остановить сервисы"
	@echo "  make clean     Удалить контейнеры, сети и тома"
	@echo "  make logs      Показать логи сервиса (по умолчанию: api, можно указать другой через 'make logs service=db')"
	@echo "  make ps        Список сервисов и их статус"
	@echo "  make help      Показать это сообщение"
	@echo ""
